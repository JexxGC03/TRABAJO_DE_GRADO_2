
  # Crear web con React

  This is a code bundle for Crear web con React. The original project is available at https://www.figma.com/design/yO95S6gB0iatvM9e6Y3ggi/Crear-web-con-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  